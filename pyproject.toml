[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "atlas-framework"
version = "1.0.0-alpha"
description = "Experimental framework that helps AI and Codex discover and reuse validated engineering capabilities in software projects."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.11"
dependencies = [
  "openpyxl>=3.1,<4",
]

[project.scripts]
atlas = "atlas_gateway.cli:main"

[tool.setuptools.packages.find]
where = [".", "packages/ai-execution/src", "packages/enterprise-intake/src", "packages/file-lifecycle/src", "packages/knowledge-intake/src", "packages/operation-outcome/src", "packages/runtime-config/src", "packages/tabular-input/src"]
include = ["atlas_gateway*", "atlas_consumer*", "atlas_ai_execution*", "atlas_enterprise_intake*", "atlas_file_lifecycle*", "atlas_knowledge_intake*", "atlas_operation_outcome*", "atlas_runtime_config*", "atlas_tabular*"]

[tool.setuptools.package-data]
atlas_gateway = [
  "resources/*.json",
  "resources/skills/atlas-gateway/*",
  "resources/skills/atlas-gateway/**/*",
]
