# Enterprise Intake Synthetic Example

This public-safe example shows the narrow Enterprise Intake reference flow:

`Tabular input -> project adapter -> preview -> row decision/issues -> commit readiness`

Run:

```bash
python examples/enterprise-intake-synthetic/run_example.py
```

It does not write to a database.
